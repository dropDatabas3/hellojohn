# ───────── Environment files ─────────
.env
.env.*
!.env.example
configs/config.yaml

# ───────── Control-plane local data ─────────
/data/

# ───────── Local utility scripts (keep out of repo) ─────────
codeprinter.bat
dump_code.ps1
dump_code_*.txt
code_dump_*.txt
seed_data.*


# ───────── OS / Tooling ─────────
*.log
*.swp
*.DS_Store
Thumbs.db

# ───────── Build / Cache (future) ─────────
bin/
dist/
coverage/

security_password_blacklist.txt
