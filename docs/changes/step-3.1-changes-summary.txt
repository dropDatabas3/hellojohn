FASE 3 - PASO 3.1: Mejoras al Middleware RequireAdminTenantAccess()

ARCHIVO: internal/http/middlewares/admin.go

CAMBIOS REALIZADOS:

1. Import agregado:
   - Agregado import "log" para logging de seguridad

2. Mejoras en extractTenantID():
   - ANTES: Usaba parsing manual del path con strings.Split()
   - DESPUÉS: Usa r.PathValue("tenant_id") como método primario (consistente con FASE 2)
   - Orden de prioridad estandarizado:
     1. Path param {tenant_id} (método estándar)
     2. Query param ?tenant_id= (fallback legacy)
     3. Query param ?tenant= (fallback legacy)

3. Logging de seguridad agregado en RequireAdminTenantAccess():

   a) Log de tenant elevation attempt (WARN):
      - Se registra cuando admin de Tenant A intenta acceder a Tenant B
      - Incluye: admin_id, admin_email, requested_tenant, allowed_tenants, path, method
      - Nivel: "warn" (para alertas de seguridad)

   b) Log de acceso exitoso (DEBUG):
      - Se registra cuando admin tenant accede exitosamente a su tenant asignado
      - Incluye: admin_type, admin_id, tenant, path
      - Nivel: "debug" (para auditoría detallada)

EVIDENCIAS:
- Diff: docs/changes/step-3.1-admin-middleware.diff (77 líneas)
- Compilación: ✅ Exitosa
- Binario: hellojohn.exe (29 MB)

CRITERIOS DE ACEPTACIÓN:
- [x] Middleware RequireAdminTenantAccess() ya existía
- [x] extractTenantID() estandarizado para usar r.PathValue("tenant_id")
- [x] Logging de tenant elevation attempts agregado
- [x] Logging de accesos exitosos agregado (debug)
- [x] Backend compila sin errores
- [x] Consistente con estandarización FASE 2

NOTAS:
- El middleware ya tenía la lógica de validación correcta (AdminType global vs tenant)
- Solo se agregó logging de seguridad y estandarización de extractTenantID()
- La validación previene efectivamente tenant elevation attacks
